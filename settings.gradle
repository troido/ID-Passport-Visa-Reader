pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven {
            url 'https://troido.jfrog.io/artifactory/troido-gradle-release'
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url 'https://troido.jfrog.io/artifactory/troido-gradle-release'
        }
    }
}
rootProject.name = "IdPassportVisareader"
include ':app'
